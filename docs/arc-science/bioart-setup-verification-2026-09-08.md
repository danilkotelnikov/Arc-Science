# BioArt setup continuation — verification

8 September 2026. **Unmerged development checkpoint; the overall repository is
not merge-ready.** This increment improves BioArt selection and Rust setup. It
does not close the inherited renderer force-cancellation defect I1 or the
previously reported 24 inherited root-suite failures.

Qualified production commit: `1da9ce7403c8bd0cb65ef85113143a2bce6a87a7`.
Continuation base: `660ef2b7bae6325caae6e1417331ab6a3c278957`.
Original Vedix baseline: `8c5807f573ffb2731ee3232fcae91da1c26b47d7`.
Application version remains 0.5.0; native supervisor remains 0.1.0. Later handoff
changes are documentation-only; the archive records its exact Git HEAD.

## Implemented and reviewed

- BioArt fetch defaults to SVG, filters for format compatibility before preferring
  an explicitly neutral source caption, and preserves deterministic source-order
  ties. Explicit representation choices remain authoritative. Original bytes,
  selected IDs, license/credit, source-page hash and receipt checks are preserved.
- Native `init --python` stores a selected interpreter without running or
  canonicalizing it. Invalid lexical directory paths reject before exclusive
  config creation; initialization never overwrites an existing config.
- Typed native `bioart search`, `inspect`, `fetch`, `verify` and `import` reuse the
  Python provider and the existing process owner. The project/cache bridge is
  singular; arguments remain data, and egress is explicit and unavailable to
  verify/import. Windows direct BioArt rejects before worker launch.
- Task 1 passed independent spec/quality review. Task 2's Important initialization
  finding T2-I1 was fixed test-first and passed scoped independent re-review.
  Whole-continuation review approved the new code/spec with one Minor whitespace
  finding M1. The single final correction in `81dc3e0` passed scoped independent
  re-review: M1 addressed, no new findings, exact whole-range whitespace check
  passed. Overall merge readiness remains blocked by the inherited issues above.

The two initialization and rendering findings are distinct: **T2-I1 is fixed;
inherited renderer I1 remains open.** Native force cancellation can terminate
Python before cleanup of its detached renderer. Passing configuration/BioArt
tests do not establish renderer containment. No process supervision code changed.

## Executed qualification

Controller checks used a fresh `git archive` of the qualified commit and a fresh
Cargo target directory, Rust 1.90.0, Linux 6.18.35 x86_64, and the already provisioned
Python 3.12.13 scientific environment. This is not a fresh-machine dependency
installation or executed Windows/macOS CI qualification.

| Check | Result | Boundary |
| --- | --- | --- |
| `cargo test --locked --offline` | 31 passed: acquisition 3, config 10, process 18 | Real local stdlib processes; no scientific equivalence or renderer-I1 claim |
| `cargo fmt --all -- --check` | Passed | Final Rust source |
| `cargo clippy --locked --offline --all-targets -- -D warnings` | Passed, no diagnostics | Fresh target; 3.04 s |
| `cargo build --release --locked --offline` | Passed | Linux x86_64; 17.05 s |
| App `PYTHONPATH=src python -m pytest -q -rs` | 557 passed, 6 skipped, 50 warnings; 24.72 s | Full app; six explicit actual-Blender-runtime skips, no missing-native-binary skip |
| UI `npm test` | 10 passed across 2 files; 3.51 s | Unchanged HeroUI DOM regression; no new browser pixel review or UI build |
| Python wheel build and extracted-wheel HTTP checks | Passed; 20 public assets, sole 347,038-byte JS bundle | Exact hashes/lengths, closed example allowlist, authenticated mission access |
| Actual installed wheel versus final source | Every package file byte-identical, excluding generated bytecode | Installed into a fresh target; provisioned environment supplies dependencies |
| Final native → installed wheel | Offline fetch → verify → import passed | Nondefault cache; literal entry 18 / group 64 / SVG file 626860; source and receipt hashes checked |
| Retained complete NIH HTML | Parsed entry 18's 11 groups, entry 505, seven search hits | Initial client-only search shell rejects; no new NIH file transfer |
| Original plugins/root tests and frozen visuals | Git tree/byte checks unchanged | Prior legacy results are retained, not rerun or declared repaired |

The app's 50 warnings are existing scikit-image/NumPy shape-assignment
deprecations. npm reported its existing proxy-configuration warning. Pip reported
an invalid-distribution warning from the reused environment during successful
wheel build/install. No blanket suppression, proxy alteration or environment-file
deletion was used to obtain these results.

The earlier c83b061 qualification is retained under `evidence/pre-fix-c83b061`,
explicitly marked pre-fix. The Python application did not change in the scoped
native fix, so its already checked wheel is retained; the final native smoke test
independently compared that installed package to the final source. Final native
tests, build and application integration were rerun after the fix.

## Distribution identities

| Artifact | Bytes | SHA256 |
| --- | ---: | --- |
| `arc-science-native-linux-x86_64` | 1,240,024 | `7a595d5a3a4b17590635197586905ad03d1ac7025e8e4e25f3a77978975c30d7` |
| `arc_science-0.5.0-py3-none-any.whl` | 3,762,125 | `b8726bf84822e3b49a7400aae3ebc174cbfd2b38a0ddf15f9bdc162f97cdb2fe` |

The wheel was built with `pip wheel --no-deps --no-build-isolation --no-index`
and installed with `pip install --no-deps --no-index --target` into a fresh
directory. Its actual import location was asserted, as were final source bytes.
Native used `init --python` with the provisioned interpreter and a nondefault
cache path containing spaces and Unicode. No `--allow-egress` was supplied.

**The integration SVG is a synthetic white rectangle, not NIH artwork.** Metadata
comes from the source-derived entry-18 fixture; test timestamps and receipts are
generated locally. Its SHA256 is
`bc12cb08e1bdd4a377996e3ff1ef2f7064562b6cb4caa10065b5ae714c512e4f`.
The imported manifest still reports `rights_verified=false` and
`scientific_validity_established=false`. This proves bridge/integrity/import
behavior, not source authenticity, live compatibility or scientific validity.
SVG eligibility/import used the explicitly provisioned `vector` dependencies.

## Native resource measurements

C `posix_spawn`, `CLOCK_MONOTONIC` and `wait4`: three warmups followed by 30 samples
per command in the shared Linux container, without affinity/frequency/cache
control. The source and raw CSV samples are retained.

| Command | Minimum / median / maximum, ms | Maximum peak RSS, KiB |
| --- | ---: | ---: |
| `config` | 0.769198 / 0.853754 / 1.171324 | 1,924 |
| `doctor` | 0.777571 / 0.926557 / 1.627589 | 1,892 |

These commands do not start Python or Blender. `doctor` checks executable
presence, not imports or scientific validity. These measurements are not
scientific-workload memory/speedup results or cross-platform performance evidence.

## Preserved figures, research and access boundaries

The existing 1DQJ white collage, muted blue/grey palette, three transparent views,
source coordinates and 49-pair geometric contact table remain unchanged. The PNG
hash remains `7394f162dca9bba20ca985a2cd43473da2c9df7d88be5e39acaec81f30f598d9`.
SVG, source CIF and captured worker hashes also match the prior checkpoint. No
new Blender render or live visual-model comparison was run. Contacts are heavy-atom
proximities, not affinity, hydrogen-bond assignments or measured MD results.

GitHub repository/commit reads succeeded, but one development-branch write
returned HTTP403 `Resource not accessible by integration`. No remote branch,
commit, rename or sync was made; no alternate credentials were used. The user's
explicit no-sync fallback governs this checkpoint.

Live NIH vector transfer, Windows BioArt, Windows/macOS runtime execution,
automatic browser fallback, a new Superdesign draft, browser pixel review, live
vision comparison, Lean proof checking, full Rust scientific rewrite, signed
desktop installers and the broader self-evolving composition editor remain
unavailable, unimplemented or unqualified. The
[research addendum](bioart-setup-research-2026-09-08.md) records named integrations'
actual contributions and source-grounded recommendations. The
[previous verification](verification-2026-09-08.md) retains historical render,
legacy-test and original-development evidence.

## Rulings I made

1. Continue the accepted BioArt/native design and defer renderer I1 because the
   user redirected this increment to BioArt and portable setup. Cost: renderer
   containment remains deferred and prevents merge qualification.
2. Use a separate local continuation branch in the current project and retain all
   intermediates. Cost: an additional branch and larger archive to reconcile.
