# Program — 19 September 2026: identity, empty workbench, Playwright, OAuth seats, HoH features, humanizer

Requested by the user on 2026-09-19 after the memory-scale loop; scope questions were
answered explicitly (recorded here so later loops do not reopen them). Source context:
the two original GPT conversations were read in the built-in browser this time
(`…/share/6aaa6001…` design review; `…/share/6aaa5f66…` figure-style and memory
requirements) and the start prompt from Claude session `dc5a505b` (2026-09-16).

## Decisions

| Item | Decision |
| --- | --- |
| Logo | Integrate `SNOGGO_logo_master.svg`: the mark's inner "puddles" filled white, outside transparent. Window icon, executable icon, workbench header and favicon. |
| Binary | The desktop executable is `Arc Science.exe`. |
| Default collages | Remove the packaged 1DQJ / HyHEL-63 example entirely; Molecules opens on the operator's own renders. |
| Playwright | Add `@playwright/test` (user authority for the dependency) and encode the browser-QA checklist as end-to-end tests against the real service. |
| OAuth | Claude subscription OAuth for the model seats (Opus/Sonnet 5): correct headers, token handling, live probe, honest report if Anthropic rejects such tokens for a third-party service. |
| HoH features | All four from the design review: (a) explicit check states `satisfied / failed / unknown / error / stale / not_applicable` with a fail-closed release decision; (b) consecutive figure-repair cycles; (c) claim-strength adjustment; (d) change-effect declarations. |
| Humanizer | text2go MCP `detect` (AI-detection + readability via `api.edgeshop.ai`, no key, text leaves the machine) by default under egress consent and bounded text, plus a local prose-only rewrite that must preserve numbers, identifiers and citations. Output is never labelled human-written. |

## Loops, in order

Each loop follows the HoH cycle (plan → develop → independent evaluation by Sol on the
frozen diff → integrate → record) and keeps every suite green.

1. **Identity and packaging** — logo asset with white puddles, window icon, executable
   icon resource (Windows SDK `rc.exe`, no new crate), header mark and favicon,
   `Arc Science.exe`, launcher and docs.
2. **Empty workbench** — delete the example assets and panel, empty state, tests and
   packaging contract updated.
3. **Playwright** — config that starts the real service on a fixture data directory;
   specs for every row of `2026-09-18-browser-qa.md` that does not need Blender or the
   public network, plus the new empty state and download notice.
4. **Claude OAuth seats** — `anthropic-beta` header for the oauth style, token file
   expiry/refresh handling, `/api/capabilities` live probe, mock-provider tests, then
   a live check with an operator-supplied token.
5. **HoH (a)** check states and release decision; 6. **HoH (b)** repair cycles;
   7. **HoH (c)** claim-strength adjustment; 8. **HoH (d)** change-effect declarations —
   each with engine tests, UI and Playwright coverage.
9. **Humanizer** — connector with egress consent, bounded text, provenance and
   review-record entry; local rewrite with preservation check; defaults and controls.

## Preserve

Everything in `AGENTS.md`: tokens never in URLs/logs, one worker per data directory,
`PYTHONUTF8=1`, no publishing, Lore-style commits, unverified gates recorded rather
than claimed. The accepted candidate's suites stay green after every loop.
